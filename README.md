# PD Bot

## 1. Обзор проекта

PD Bot - это многофункциональный Discord бот, разработанный на Python с использованием библиотеки discord.py. Бот предоставляет широкий спектр возможностей, включая:

*   Интеграцию с Dota 2 API для отображения статистики матчей
*   Музыкальный плеер с поддержкой YouTube
*   Развлекательные команды и симуляцию битв между пользователями
*   Систему розыгрышей с автоматическим выбором победителя
*   Административные инструменты для модерации сервера
*   Отслеживание игровой активности пользователей
*   Систему ролей по реакциям для автоматической выдачи ролей

Бот использует гибридные команды, поддерживающие как традиционный префиксный синтаксис (`!команда`), так и Discord slash-команды (`/команда`).

## 2. Архитектура

Проект построен на модульной архитектуре, использующей систему когов (Cogs) discord.py для организации команд и функциональности.

```bash
pd_bot/
├── main.py                # Точка входа приложения
├── config.py              # Управление конфигурацией
├── pyproject.toml         # Конфигурация проекта, инструментов и зависимостей
├── LICENSE                # Лицензия проекта
├── README.md              # Документация проекта
├── assets/                # Ресурсы проекта
│   └── deathbattle.jpg    # Изображение для команды deathbattle
├── coverage/              # Отчеты о покрытии кода тестами
├── cogs/                  # Модули команд (Cogs)
│   ├── admin.py           # Админ команды (clear, kick, restart)
│   ├── activity.py        # Отслеживание и статистика активности
│   ├── anime.py           # Автопостинг аниме-артов
│   ├── fun.py             # Развлекательные команды (deathbattle, snipe, penis, avatar)
│   ├── giveaway.py        # Розыгрыши
│   ├── lastmatch.py       # Команды Dota 2 (lastmatch)
│   ├── links.py           # Привязка аккаунтов Dota 2 (link, unlink, links)
│   ├── logging_cog.py     # Пересылка логов в Discord канал
│   ├── music.py           # Музыкальные команды (play, skip, stop, etc.)
│   ├── role_reaction.py   # Система ролей по реакциям
│   └── update.py          # Команда обновления бота
├── docs/                  # Документация
│   ├── CONTRIBUTING.md    # Руководство по контрибьюции
│   ├── STYLE_GUIDE.md     # Руководство по стилю кода
│   └── UNIFICATION_PLAN_DETAILED.md # Подробный план унификации проекта
├── handlers/              # Обработчики событий
│   ├── events.py          # Обработчики событий Discord (on_ready, on_presence_update, etc.)
│   └── message_handler.py # Обработчик текстовых сообщений (если нужен)
├── utils/                 # Вспомогательные функции
│   ├── activity_data_manager.py # Управление данными активности
│   ├── avatar_utils.py
│   ├── deathbattle_utils.py
│   ├── dota_api.py
│   ├── dota_match_utils.py
│   ├── dota_utils.py
│   ├── error_handler.py
│   ├── links_data_manager.py
│   ├── message_utils.py
│   ├── penis_utils.py
│   ├── role_reaction_data_manager.py
│   ├── snipe_utils.py
│   └── music/              # Музыкальный модуль
│       ├── __init__.py
│       ├── config.py       # Конфигурация музыкального модуля
│       ├── embeds.py       # Создание эмбедов для музыкальных команд
│       ├── player.py       # Класс MusicPlayer для управления воспроизведением
│       ├── ui.py           # Интерактивные компоненты UI
│       └── yt_integration.py # Интеграция с YouTube
├── data/                  # Данные и кэширование
│   ├── config.json        # Файл конфигурации (ВАЖНО: не хранить в Git)
│   ├── bot_data.db        # База данных SQLite (привязки, статистика активности)
│   ├── cache/             # Кэш API (match_cache.json, items_cache.json)
│   └── activity_archives/ # (Устарело, данные теперь в bot_data.db)
├── downloads/             # Скачанные музыкальные треки (временные)
└── logs/                  # Логи работы бота (отдельный файл для каждого запуска)
```
## 3. Технические особенности

*   **Загрузка конфигурации (`config.py`, `main.py`):**
    *   Основная конфигурация (`BOT_TOKEN`, `STRATZ_API_KEY`, `PREFIX` и др.) загружается из `data/config.json` функцией `load_config()`.
    *   Привязки аккаунтов Dota 2 и статистика активности хранятся в базе данных SQLite `data/bot_data.db`. Управление данными осуществляется через `utils/links_data_manager.py` и `utils/activity_data_manager.py`.
    *   Загруженная конфигурация (`config`) прикрепляется к объекту `bot` в `main.py` (`bot.config`), делая ее доступной для всех когов.
*   **Инициализация БД (`main.py`, `utils/database.py`):**
    *   При запуске бота функция `initialize_database()` проверяет/создает файл `data/bot_data.db` и необходимые таблицы (`links`, `daily_activity`, `monthly_activity`, `role_reactions`).
*   **Загрузка модулей (`main.py`):**
    *   Функция `load_cogs()` динамически сканирует директорию `cogs/` и загружает все `.py` файлы (кроме `__init__.py`) как расширения (коги).
    *   Обработчики событий (`handlers.events`) и сообщений (`handlers.message_handler`) загружаются явно.
*   **Обработка ошибок (`utils/error_handler.py`):**
    *   Декоратор `@command_error_handler` используется для всех команд для унифицированного перехвата и логирования исключений.
    *   Функции `safe_send()` и `safe_send_error()` обеспечивают отправку сообщений пользователю с учетом типа команды (slash или префиксная) и состояния взаимодействия.
*   **Взаимодействие с API Dota 2 (`utils/dota_api.py`, `dota_match_utils.py`):**
    *   Используется API Stratz через GraphQL-запросы.
    *   Реализовано кэширование ответов API (матчи, предметы) в памяти (с TTL) и на диске (`data/cache/`) для снижения нагрузки и ускорения ответов.
    *   Используется механизм повторных запросов (`query_api_with_retry`) при ошибках API.
*   **Музыкальный плеер (`cogs/music.py`, `utils/music/`):**
    *   Полностью переработан с использованием современных подходов `discord.py`.
    *   Использует `yt-dlp` для поиска (YouTube) и скачивания аудио (предпочтительный кодек - Opus).
    *   Воспроизведение осуществляется через `discord.FFmpegPCMAudio`.
    *   Модульная структура в директории `utils/music/`:
        *   `player.py`: Класс `MusicPlayer` для управления воспроизведением и очередью
        *   `ui.py`: Интерактивные компоненты UI (кнопки, выпадающие списки)
        *   `embeds.py`: Создание эмбедов для музыкальных команд
        *   `yt_integration.py`: Функции для работы с YouTube (поиск, скачивание)
        *   `config.py`: Конфигурация музыкального модуля (логгер, опции FFmpeg)
    *   Ког `MusicCog` (`cogs/music.py`) управляет единственным экземпляром `MusicPlayer` (т.к. бот рассчитан на один сервер).
    *   Реализованы интерактивные компоненты (`discord.ui`):
        *   Результаты поиска команды `/play` отображаются в виде выпадающего списка (`Select`).
        *   Под сообщением "Сейчас играет" отображаются кнопки управления (`Buttons`: Пауза/Возобновить, Пропустить, Стоп, Очередь).
    *   Скачанные аудиофайлы временно хранятся в `downloads/` и автоматически удаляются после воспроизведения или при полной остановке плеера.
    *   Реализовано логирование действий плеера через настроенный логгер в `config.py`.
*   **Отслеживание активности (`cogs/activity.py`, `utils/activity/`):**
    *   `cogs/activity.py`: Содержит основной класс кога `ActivityTracker`, команды, обработчики событий Discord и запуск фоновых задач.
    *   `utils/activity_data_manager.py`: Управляет взаимодействием с таблицами `daily_activity` и `monthly_activity` в `data/bot_data.db`.
    *   `utils/activity/views.py`: Содержит классы `discord.ui.View` (`ActivityView`, `StatsView`) для интерактивных отчетов и статистики.
    *   `utils/activity/reports.py`: Содержит основную логику генерации и отправки ежедневных/ежемесячных отчетов, вызываемую фоновыми задачами и командами ручного запуска.
    *   `utils/activity/helpers.py`: Вспомогательные функции (форматирование времени, проверка ботов).
   *   Активность отслеживается через `on_presence_update` в коге `ActivityTracker`.
   *   Данные о текущих сессиях хранятся в памяти кога и периодически (`periodic_save`) или при завершении сессии записываются в `daily_activity` через `ActivityDataManager`.
   *   Фоновая задача `daily_report` (в 00:00 МСК) вызывает логику из `utils/activity/reports.py`, которая отправляет отчет за вчерашний день, а затем вызывает `ActivityDataManager` для переноса данных `daily -> monthly` и очистки `daily`.
   *   Фоновая задача `monthly_report` (в 12:00 МСК 1-го числа) вызывает логику из `utils/activity/reports.py` для отправки отчета за предыдущий месяц.
   *   Используется ID канала для отчетов из `config.json` (`REPORT_CHANNEL_ID`), если он указан, иначе используется значение по умолчанию.
*   **Механизм обновления (`cogs/update.py`):**
    *   Команда `/update` выполняет `git pull` и инициирует перезапуск бота через пользовательский сервис `systemd`.
    *   **Требования:** Бот должен быть настроен и запущен как пользовательский сервис `systemd` (например, `discord-bot.service`). Пользователь, от имени которого запущен бот, должен иметь права на перезапуск этого сервиса командой `systemctl --user restart discord-bot.service`. Имя сервиса (`discord-bot.service`) жестко задано в коде кога.

Бот инициализируется в `main.py`, который загружает конфигурацию из `data/config.json`, прикрепляет ее к объекту бота, настраивает логирование и запускает все коги и обработчики.

## 3. Установка и запуск

### Конфигурация проекта

Проект использует `pyproject.toml` для централизованной конфигурации инструментов разработки:

- **black**: Форматирование кода с максимальной длиной строки 100 символов
- **isort**: Сортировка импортов с профилем, совместимым с black
- **mypy**: Проверка типов с настройками для различных модулей
- **flake8**: Проверка стиля кода с настройками для исключения определенных правил
- **pytest**: Конфигурация для тестирования (asyncio_mode, filterwarnings)

Все настройки инструментов находятся в одном файле, что упрощает поддержку и обеспечивает согласованность.

### 3.1 Требования

*   Python 3.13+
*   FFmpeg (для музыкального плеера)

### 3.2 Установка зависимостей

Все зависимости (основные и для разработки) определены в pyproject.toml. Для установки выполните:

```bash
pip install -e .[dev]
```

### 3.3 Настройка конфигурации

Создайте файл `data/config.json` со следующим содержимым (замените значения-заполнители):

```json
{
  "BOT_TOKEN": "ваш-токен-discord-бота",
  "STRATZ_API_KEY": "ваш-токен-stratz-api",
  "PREFIX": "!",
  "PROXY_URL": "",
  "REPORT_CHANNEL_ID": 573665353327181824,
  "ANIME_CHANNEL_ID": 298811309640646666,
  "TWITCH_CLIENT_ID": "ваш-client-id-twitch-api",
  "TWITCH_CLIENT_SECRET": "ваш-client-secret-twitch-api"
}
```
*   `BOT_TOKEN`: Токен вашего Discord бота.
*   `STRATZ_API_KEY`: Ключ API Stratz (для Dota 2).
*   `PREFIX`: Префикс для текстовых команд (например, "!"). Бот загружает этот префикс при запуске. Если ключ отсутствует, используется "!" по умолчанию.
*   `PROXY_URL`: URL прокси-сервера для yt-dlp (опционально).
*   `REPORT_CHANNEL_ID`: ID канала для отчетов активности. Бот использует это значение для отправки ежедневных и ежемесячных отчетов. Если ключ отсутствует, используется значение по умолчанию `573665353327181824`.
*   `ANIME_CHANNEL_ID`: ID текстового канала для автоматической публикации аниме-изображений.
*   `TWITCH_CLIENT_ID`: Client ID приложения Twitch для API (необходим для модуля отслеживания стримов).
*   `TWITCH_CLIENT_SECRET`: Client Secret приложения Twitch для API (необходим для модуля отслеживания стримов).
*   `LOGGING_CHANNEL_ID`: (Опционально) ID канала для пересылки логов бота. Если отсутствует, используется значение по умолчанию `1365045098785542224`.
*   `ROLE_REACTION_DEFAULT_CHANNEL_ID`: (Опционально) ID канала по умолчанию для создания сообщения с ролями командой `/setup_role_message`. Если отсутствует, используется канал, из которого вызвана команда.
*   `ACTIVITY_MIN_RECORD_THRESHOLD_SECONDS`: (Опционально) Минимальная длительность игровой сессии в секундах для её записи в статистику (по умолчанию: `10`).
*   `ACTIVITY_MAX_RECORD_THRESHOLD_SECONDS`: (Опционально) Максимальная длительность игровой сессии в секундах для отсечения аномально долгих сессий (по умолчанию: `172800`, т.е. 2 дня).
*   `ACTIVITY_MONTHLY_REPORT_MIN_TIME_SECONDS`: (Опционально) Минимальное время игры в секундах для включения в список игр пользователя в ежемесячном отчете (по умолчанию: `1800`, т.е. 30 минут).

**ВАЖНО:** Не добавляйте файлы `data/config.json` и `data/bot_data.db` в систему контроля версий (Git). Убедитесь, что `data/` добавлен в `.gitignore`.

### 3.4 Запуск бота

```bash
python main.py
```

Для непрерывной работы рекомендуется использовать менеджер процессов, такой как `systemd` (в режиме пользовательского сервиса) или `supervisor` (см. раздел 5.8 для команды `update`).

## 4. Документация

Проект содержит подробную документацию в директории `docs/`:

- **[CONTRIBUTING.md](docs/CONTRIBUTING.md)**: Руководство по контрибьюции, содержащее правила и стандарты разработки
- **[STYLE_GUIDE.md](docs/STYLE_GUIDE.md)**: Подробное руководство по стилю кода, включая правила форматирования, именования и документирования
- **[UNIFICATION_PLAN_DETAILED.md](docs/UNIFICATION_PLAN_DETAILED.md)**: Подробный план унификации проекта

## 5. Модули (Cogs) и Команды

### 5.1 Административный (`cogs/admin.py`)

*   `/clear [count] [user]`: Удаление сообщений (до 100, обходит 14-дневный лимит).
*   `/kick <member> [reason]`: Исключение пользователя с сервера.
*   `/restart`: (Только для владельца) Перезапуск бота через systemd (устарело, используйте `/update`).

### 5.2 Аниме (`cogs/anime.py`)

*   Автоматически публикует случайные SFW аниме-изображения в канал, указанный в `ANIME_CHANNEL_ID`, дважды в день (10:00 и 18:00 UTC).
*   `/post_anime`: (Только для администраторов) Ручной запуск публикации аниме-изображения.

### 5.3 Отслеживание активности (`cogs/activity.py`, `utils/activity/`)

Модуль отслеживает игровую активность пользователей и предоставляет статистику. Логика разделена между когом и вспомогательными утилитами.

*   `/activity [test_mode]`: (Админ) Показывает текущую дневную статистику. Использует интерактивное представление (`ActivityView`) с пагинацией и переключением режимов (пользователи/игры).
*   `/mystats [user] [month] [year]`: Показывает статистику пользователя за указанный месяц (или текущий). Использует интерактивное представление (`StatsView`) с пагинацией.
*   `/mystatsall [user]`: Показывает статистику пользователя за всё время. Использует интерактивное представление (`StatsView`) с пагинацией.
*   `/report_daily <year> <month> <day>`: (Админ) Вручную отправляет ежедневный отчет за указанную дату в канал отчетов. **Не выполняет перенос данных.**
*   `/report_monthly <year> <month>`: (Админ) Вручную отправляет ежемесячный отчет за указанный месяц и год в канал отчетов.
*   **Автоматические отчеты:**
    *   Ежедневный отчет (за вчера) отправляется в 00:00 МСК. После отправки данные переносятся из дневной в месячную статистику.
    *   Ежемесячный отчет (за прошлый месяц) отправляется 1-го числа в 12:00 МСК.
    *   Канал для отчетов определяется `REPORT_CHANNEL_ID` в `config.json`.
*   **Хранение данных:**
    *   `data/bot_data.db`: База данных SQLite, таблицы `daily_activity` и `monthly_activity`. Управляется через `utils/activity_data_manager.py`.
*   **Конфигурация:** Пороги минимального/максимального времени записи сессии и минимального времени для ежемесячного отчета настраиваются в `data/config.json` (см. раздел 3.3).
*   **Вспомогательные модули:**
    *   `utils/activity/views.py`: Интерактивные представления.
    *   `utils/activity/reports.py`: Логика генерации и отправки отчетов.
    *   `utils/activity/helpers.py`: Форматирование времени и др.

### 5.4 Развлечения (`cogs/fun.py`)

*   `/deathbattle [member1] [member2]`: Симуляция битвы.
*   `/snipe`: Показ последнего удаленного сообщения в канале (кэш в памяти, сбрасывается при перезапуске).
*   `/penis [user]`: Измерение "пениса".
*   `/avatar [user]`: Отображение аватара.

### 5.5 Розыгрыши (`cogs/giveaway.py`)

*   `/giveaway <duration> <description>`: Создание розыгрыша.
*   **Примечание:** Текущая реализация не сохраняет активные розыгрыши при перезапуске бота.

### 5.6 Dota 2 Интеграция (`cogs/lastmatch.py`, `cogs/links.py`)

*   `/link <player_id>`: Привязка Steam ID Dota 2 к Discord аккаунту (до 5 аккаунтов). Данные хранятся в `data/bot_data.db`.
*   `/unlink [player_id]`: Отвязка Steam ID (если ID не указан, отвязываются все).
*   `/links`: Показ привязанных аккаунтов.
*   `/lastmatch [member]`: Отображение детальной информации о последнем матче Dota 2 (автоматически выбирается аккаунт с последней игрой). Использует API Stratz.

### 5.7 Музыка (`cogs/music.py`, `utils/music/`)

Модуль полностью переписан для повышения стабильности и добавления интерактивности. Используются slash-команды и компоненты `discord.ui`. Логика разделена на модули в директории `utils/music/`.

*   `/play <query>`: Воспроизведение музыки по URL (YouTube, SoundCloud и др. поддерживаемые `yt-dlp`) или поисковому запросу на YouTube. При поиске отображает интерактивный список результатов для выбора.
*   `/skip`: Пропускает текущий трек. Может быть вызван через slash-команду или кнопку под плеером.
*   `/stop`: Полностью останавливает воспроизведение, очищает очередь, удаляет временные файлы и отключает бота от голосового канала. Может быть вызван через slash-команду или кнопку.
*   `/pause`: Ставит воспроизведение на паузу. Может быть вызван через slash-команду или кнопку.
*   `/resume`: Возобновляет воспроизведение после паузы. Может быть вызван через slash-команду или кнопку.
*   `/queue`: Показывает текущий трек и очередь воспроизведения (как эфемерное сообщение). Может быть вызван через slash-команду или кнопку.
*   `/volume <0-200>`: Устанавливает громкость воспроизведения в процентах.
*   **Интерактивное управление:** Сообщение "Сейчас играет" содержит кнопки для быстрого доступа к `pause`/`resume`, `skip`, `stop`, `queue`.
*   **Модульная архитектура:**
    * `player.py`: Класс `MusicPlayer` для управления воспроизведением и очередью
    * `ui.py`: Интерактивные компоненты UI (кнопки, выпадающие списки)
    * `embeds.py`: Создание эмбедов для музыкальных команд
    * `yt_integration.py`: Функции для работы с YouTube (поиск, скачивание)
    * `config.py`: Конфигурация музыкального модуля (логгер, опции FFmpeg)
*   **Автоотключение:** Бот автоматически покидает голосовой канал, если остается в нем один (логика реализована в `on_voice_state_update` внутри `handlers/events.py`).

### 5.8 Обновление (`cogs/update.py`)

*   `/update`: (Только для владельца бота) Получает последние изменения из Git (`git pull`) и перезапускает бота через пользовательский сервис `systemd`.
*   **Требования:** Бот должен быть настроен и запущен как пользовательский сервис `systemd` (например, `discord-bot.service`). Пользователь, от имени которого запущен бот, должен иметь права на перезапуск этого сервиса командой `systemctl --user restart discord-bot.service`. Имя сервиса (`discord-bot.service`) жестко задано в коде.

### 5.9 Роли по реакциям (`cogs/role_reaction.py`)

*   `/setup_role_message`: (Только для администраторов) Создает сообщение для получения ролей через реакции. Канал для сообщения определяется ключом `ROLE_REACTION_DEFAULT_CHANNEL_ID` в `data/config.json`, или используется текущий канал, если ключ не задан.
*   `/role_assign <role> <emoji> <description>`: (Только для администраторов) Добавляет роль, которую можно получить через реакцию. Требуется указать роль, эмодзи и описание роли.
*   `/role_remove <emoji>`: (Только для администраторов) Удаляет роль из списка ролей, получаемых через реакции.
*   **Автоматическая выдача ролей:** Пользователи получают роли при нажатии на соответствующие эмодзи и теряют их при удалении реакций.
*   **Хранение данных:** Привязки ролей к эмодзи хранятся в базе данных SQLite (`data/bot_data.db`) в таблице `role_reactions`.

### 5.10 Логирование (`cogs/logging_cog.py`)

*   Этот ког не предоставляет команд пользователю.
*   При каждом запуске бота отправляет весь текущий лог-файл (создаётся в папке `logs/` с уникальным именем) в заданный Discord канал, а затем автоматически пересылает все новые строки, которые появляются в этом файле (реализация tail -f).
*   **Примечание:** ID канала для логирования задается ключом `LOGGING_CHANNEL_ID` в конфигурационном файле `data/config.json`. Если ключ отсутствует, используется значение по умолчанию.

## 6. Обработка ошибок (`utils/error_handler.py`)

*   Используется декоратор `@command_error_handler` для унифицированной обработки ошибок в большинстве команд.
*   Стандартные ошибки (недостаточно прав, неверный аргумент и т.д.) обрабатываются автоматически.
*   Необработанные ошибки логируются.
*   Функция `safe_send_error` используется для отправки сообщений об ошибках пользователю (учитывая slash-команды).

## 7. Кэширование (`utils/dota_api.py`)

*   **API Dota 2:** Результаты запросов к API Stratz кэшируются в памяти (с TTL) и на диске (`data/cache/`) для уменьшения нагрузки на API.
*   **Привязки аккаунтов:** Хранятся в базе данных SQLite (`data/bot_data.db`).
*   **Статистика активности:** Хранится в базе данных SQLite (`data/bot_data.db`).
*   **Snipe:** Удаленные сообщения кэшируются в памяти (`utils/snipe_utils.py`).

## 8. Логирование: Единый стандарт

В проекте используется централизованная иерархическая система логирования, обеспечивающая единообразие во всех модулях.

### 8.1 Конфигурация логирования

Логирование настраивается в `main.py` с помощью `logging.basicConfig`:

```python
import logging

from pathlib import Path
from datetime import datetime

LOGS_DIR = Path("logs")
LOGS_DIR.mkdir(exist_ok=True)
log_filename = datetime.now().strftime("%Y-%m-%d_%H-%M-%S.log")
log_path = LOGS_DIR / log_filename

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler(log_path, mode="a", encoding=None, delay=False, buffering=1),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger("bot")
```

- Все логи пишутся в отдельный файл в папке `logs/` для каждого запуска бота (например, `logs/2025-04-30_01-16-00.log`) и выводятся в консоль.
- Формат логов: время, имя логгера, уровень, сообщение.

### 8.2 Именование логгеров

Для единообразия используйте иерархические имена логгеров:

- **Основной логгер:** `"bot"` — для общих модулей, когов, обработчиков.
- **Подсистемы:** `"bot.music"`, `"bot.dota"`, `"bot.database"`, `"bot.links_db"`, `"bot.activity_db"` и т.д.
- **Dota-модули:** используйте `"bot.dota"` вместо `"dota_bot"` (требуется обновить в `utils/dota_utils.py`, `utils/dota_match_utils.py`, `utils/dota_api.py`).
- **Музыкальный модуль:** используйте `"bot.music"` во всех музыкальных файлах (включая `cogs/music.py`).

**Пример инициализации логгера:**
```python
import logging
logger = logging.getLogger("bot.music")  # для музыкального модуля
```

### 8.3 Использование логгера в подмодулях

Для подмодулей рекомендуется импортировать логгер из главного модуля подсистемы:

```python
# utils/music/player.py
from .config import logger  # logger = logging.getLogger("bot.music") в config.py
```

Это обеспечивает единый источник логгирования для всей подсистемы.

### 8.4 Пример использования

```python
logger.info("Бот запущен")
logger.warning("Внимание: что-то пошло не так")
logger.error("Ошибка при выполнении команды", exc_info=True)
```

### 8.5 Пересылка логов в Discord

- При каждом запуске бота весь текущий лог-файл (из папки `logs/`) автоматически отправляется в специальный Discord-канал через ког `cogs/logging_cog.py`.
- После этого все новые строки, появляющиеся в этом файле, также автоматически пересылаются в этот канал (реализация tail -f).
- ID канала для логирования задается ключом `LOGGING_CHANNEL_ID` в `data/config.json`.

### 8.6 Требования к новым модулям

- Всегда определяйте логгер в начале файла с правильным иерархическим именем.
- Для подмодулей используйте импорт логгера из главного модуля подсистемы.
- Не используйте print для отладки — только логгер.

**Пример для нового модуля:**
```python
import logging
logger = logging.getLogger("bot.subsystem")
```
## 9. Отслеживание Twitch-стримов (`cogs/twitch.py`)

*   `/twitch_add <twitch_username> [channel]`: (Только для администраторов) Добавляет Twitch-стримера для отслеживания. Если канал не указан, уведомления будут отправляться в текущий канал.
*   `/twitch_remove <twitch_username>`: (Только для администраторов) Удаляет Twitch-стримера из отслеживаемых.
*   `/twitch_list`: Показывает список отслеживаемых Twitch-стримеров.
*   **Автоматические уведомления:** Бот автоматически отправляет уведомления в указанные каналы, когда отслеживаемые стримеры начинают стрим.
*   **Требования:** Для работы модуля необходимо указать `TWITCH_CLIENT_ID` и `TWITCH_CLIENT_SECRET` в файле конфигурации `data/config.json`. Эти ключи можно получить, создав приложение на [Twitch Developer Console](https://dev.twitch.tv/console/apps).
*   **Хранение данных:** Информация о стримерах хранится в базе данных SQLite (`data/bot_data.db`) в таблице `twitch_streamers`.

## 10. Расширение функциональности

Для добавления новых команд:

1.  Создайте новый файл кога в директории `cogs/` (например, `cogs/my_cog.py`).
2.  Напишите класс, наследующий `commands.Cog`.
3.  Добавьте методы команд с декораторами `@commands.hybrid_command()` и `@command_error_handler`.
4.  Добавьте асинхронную функцию `setup(bot)` в конце файла для регистрации кога.
5.  Бот автоматически загрузит новый ког при следующем запуске.

## 11. Тестирование

В проекте реализовано автоматическое тестирование всех функций и методов из директории `utils/`.

### 11.1 Структура тестов

```
pd_bot/
├── tests/
│   └── test_utils/
│       ├── test_activity_data_manager.py
│       ├── test_avatar_utils.py
│       ├── test_database.py
│       ├── test_deathbattle_utils.py
│       ├── test_dota_api.py
│       ├── test_dota_match_utils.py
│       ├── test_dota_utils.py
│       ├── test_error_handler.py
│       ├── test_links_data_manager.py
│       ├── test_message_utils.py
│       ├── test_penis_utils.py
│       ├── test_role_reaction_data_manager.py
│       ├── test_snipe_utils.py
│       ├── test_twitch_api.py
│       └── test_twitch_data_manager.py
```

Каждый файл тестирует соответствующий модуль из `utils/` и покрывает все его публичные функции и методы.

### 11.2 Запуск тестов

Для запуска всех тестов используйте команду:

```bash
pytest
```

Pytest автоматически найдет все тесты в директории `tests/` и выполнит их.
Асинхронные функции тестируются с помощью `pytest-asyncio`.

### 11.3 Особенности

- Для изоляции от внешних зависимостей (БД, Discord, HTTP, файлы) используются monkeypatch и mock-объекты.
- Тесты не требуют реального подключения к Discord или к базе данных.
- Покрытие: все функции и методы из `utils/` протестированы на корректность работы с разными входными данными.
- Тесты можно расширять для других частей проекта (например, для cogs и handlers) по аналогии.

### 11.4 Пример теста

```python
import pytest
from utils.dota_utils import get_role

def test_get_role():
    assert get_role("1") == "Керри"
    assert get_role(None) == "Неизвестно"
```

### 11.5 Зачем нужны тесты

- Позволяют быстро находить ошибки при изменениях в коде.
- Гарантируют, что новые фичи не ломают существующую логику.
- Упрощают рефакторинг и поддержку проекта.
- Повышают стабильность и надежность кода.

Рекомендуется запускать тесты перед каждым коммитом и настраивать автоматический запуск через CI/CD (например, GitHub Actions).
